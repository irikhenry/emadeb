
  # Emadeb

  This is a code bundle for Emadeb. The original project is available at https://www.figma.com/design/aa5EFLrW1l8k83hIxrC0Ep/Emadeb.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  